{
  "name": "northbeam",
  "description": "Northbeam Spend Intelligence — strategic spend analysis, anomaly detection, and budget optimization via the Northbeam API",
  "version": "1.0.0",
  "author": {
    "name": "WeightCare"
  },
  "userConfig": {
    "NORTHBEAM_API_KEY": {
      "description": "Northbeam API key (from Settings → API Keys in the Northbeam dashboard)",
      "required": true,
      "sensitive": true
    },
    "NORTHBEAM_CLIENT_ID": {
      "description": "Northbeam Client ID (from Settings → API Keys in the Northbeam dashboard)",
      "required": true,
      "sensitive": true
    },
    "NORTHBEAM_API_ENV": {
      "description": "API environment: prod or uat",
      "required": false,
      "default": "prod"
    }
  }
}
