{
  "name": "northbeam",
  "description": "Northbeam Spend Intelligence — strategic spend analysis, anomaly detection, and budget optimization via the Northbeam API",
  "version": "1.0.1",
  "author": {
    "name": "Domain Methods"
  },
  "mcpServers": "./.mcp.claude.json",
  "userConfig": {
    "NORTHBEAM_API_KEY": {
      "type": "string",
      "title": "Northbeam API Key",
      "description": "Northbeam API key from Settings > API Keys in the Northbeam dashboard.",
      "required": true,
      "sensitive": true
    },
    "NORTHBEAM_CLIENT_ID": {
      "type": "string",
      "title": "Northbeam Client ID",
      "description": "Northbeam Client ID from Settings > API Keys in the Northbeam dashboard.",
      "required": true,
      "sensitive": true
    },
    "NORTHBEAM_API_ENV": {
      "type": "string",
      "title": "Northbeam API Environment",
      "description": "API environment: prod or uat.",
      "required": false,
      "default": "prod"
    }
  }
}
