[project]
name = "northbeam-plugin"
version = "1.0.0"
description = "Claude Code plugin for Northbeam spend intelligence"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24.0",
    "respx>=0.22.0",
]
